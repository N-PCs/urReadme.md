[project]
name = "urreadme-backend"
version = "0.1.0"
description = "FastAPI backend for urReadme.md — GitHub README generator"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.128.1",
    "httpx>=0.28.0",
    "pydantic>=2.11.0",
]
